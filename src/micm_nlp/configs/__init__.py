"""Configurations shipped with the package, for ``micm-nlp init-examples`` to copy out.

These are data, not code: the package exists only to give ``importlib.resources`` a
package to read the ``.yml`` files from. See :mod:`micm_nlp.cli`.
"""
