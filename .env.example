APP_NAME=micm-nlp
APP_ENV=local
SHOW_LOCALS=0

# Workspace root for artefacts (datasets, models, evals). Default for
# micm_nlp.init() when called without a root_path argument; pass
# root_path explicitly to init() to override.
PROJECT_ROOT_PATH=

WANDB_API_KEY=
HF_TOKEN=